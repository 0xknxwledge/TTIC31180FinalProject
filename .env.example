# Copy to .env and fill in. .env is gitignored — never commit real keys.
MASSIVE_KEY=            # Massive (formerly Polygon): equities / ETFs / index futures, hourly
ALPACA_KEY=             # Alpaca: equities / crypto fallback
NASDAQ_DATA_LINK_API_KEY=   # legacy QUOTEMEDIA (reusable_from_qts.py), optional
